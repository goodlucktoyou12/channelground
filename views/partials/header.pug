header.header
    .header__wrapper
        .header__column
            a(href=routes.home)
                i.fas.fa-briefcase-medical
        .header__column
            form(action="", method="")
                input(type="text", placeholder="친구검색", name="term")
        .header__column
            ul
                if !loggedUser
                    li
                        a(href=routes.join) 회원가입
                    li
                        a(href=routes.login) 로그인
                else
                    .dropdownx
                        button.dropbtnx 
                            p  구매
                        .dropdownx-content
                            a(href="") 장바구니
                            a(href="") 구매내역
                            a(href="") 문제신고
                    .dropdownx
                        button.dropbtnx 
                            p  KR
                        .dropdownx-content
                            a(href=routes.coin) 충전
                            a(href=routes.coins) 환전
                            a(href=`/users${routes.paymentTransfer}`) 환전목록
                    .loging
                        li
                            a(href=`/videos${routes.upload}`) 업로드
                        li
                            a(href=routes.me) 프로필
                        li
                            a(href=routes.logout) 로그아웃